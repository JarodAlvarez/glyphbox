/* runtime.c — top-level console state; orchestrates all modules */
#include "runtime.h"

void runtime_init(void) {
    /* Placeholder for future per-platform init (Pi, etc.) */
}

void runtime_shutdown(void) {
    /* Placeholder for future per-platform cleanup */
}
